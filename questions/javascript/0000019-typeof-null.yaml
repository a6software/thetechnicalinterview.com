---
title: What will be the output of the following code?
question: |
  ```js
  console.log(typeof(null));
  ```
hint: |
  The typeof operator is used to check the data type of a variable.
possible_answers:
  - "`null`"
  - "`object`"
  - "`undefined`"
  - "`string`"

correct_answers:
  - "`object`"

explanation: |
  The `typeof` operator returns the data type of the operand passed to it. In this case, the operand is the value of `null`. However, in JavaScript, `null` is considered an object.
tags:
  - typeof
  - JavaScript
  - data type
  - null

credit:
  - Generated by ChatGPT
