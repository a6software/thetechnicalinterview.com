title: What is a JavaScript Closure?
question: |
  A JavaScript closure is a combination of a function and the lexical environment within which that function was declared. What does this mean?
hint: |
  It's a feature of the JavaScript language.
possible_answers:
  - "It allows a function to access variables from its scope even when it is called outside of its scope."
  - "It allows a function to access variables from another function's scope even when it is called outside of its scope."
  - "It allows a function to access variables from the global scope even when it is called outside of its scope."
  - "It allows a function to access variables from its own scope even when it is called outside of its scope."

correct_answers:
  - "It allows a function to access variables from its own scope even when it is called outside of its scope."

explanation: |
  A JavaScript closure is a combination of a function and the lexical environment within which that function was declared. This means that a closure can access variables from its own scope even when it is called outside of its scope.

tags:
  - javascript
  - closure
  - function

credit:
  - "Generated by ChatGPT"
  - "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures"