---
title: JavaScript typeof operator
question: |
  What is the purpose of the `typeof` operator in JavaScript?
hint: |
  The `typeof` operator is used to determine the type of a variable in JavaScript.
possible_answers:
  - "To check if a variable is undefined"
  - "To check the type of a variable"
  - "To check if a variable is null"
  - "To check if a variable is a number"

correct_answers:
  - "To check the type of a variable"

explanation: |
  The `typeof` operator in JavaScript is used to determine the type of a variable. It can be used to check the type of a variable before performing certain operations, or to ensure that a variable is of a certain type before using it. The `typeof` operator returns a string indicating the type of the operand, for example "string", "number", "boolean" etc.

tags:
  - javascript
  - typeof
  - operator

credit:
  - "Generated by ChatGPT"
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof
