---
title: Array push
question: |
  What is the result of the following JavaScript code?
  ```js
  var numbers = [2, 4, 8];
  numbers.push(16);

  console.log(numbers);
  ```
hint: Think about the elements of the array after the `push()` function is called.
possible_answers:
  - "[2, 4, 8]"
  - "[2, 4, 8, 16]"
  - "16"
  - undefined
correct_answers:
  - "[2, 4, 8, 16]"
explanation: "After the `push()` function is called, the `numbers` array
  contains four elements: 2, 4, 8, and 16."
tags:
  - javascript
  - arrays
  - push
credit:
  - Generated by ChatGPT
